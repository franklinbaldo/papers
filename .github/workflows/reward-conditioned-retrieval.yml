name: Reward-conditioned retrieval experiment

on:
  pull_request:
    paths:
      - "relay-systems/experiments/reward-conditioned-retrieval/**"
      - ".github/workflows/reward-conditioned-retrieval.yml"
  push:
    branches: [main]
    paths:
      - "relay-systems/experiments/reward-conditioned-retrieval/**"
      - ".github/workflows/reward-conditioned-retrieval.yml"

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
          cache-dependency-path: relay-systems/experiments/reward-conditioned-retrieval/requirements.txt
      - name: Install dependencies
        run: python -m pip install -r relay-systems/experiments/reward-conditioned-retrieval/requirements.txt
      - name: Run unit tests
        working-directory: relay-systems/experiments/reward-conditioned-retrieval
        run: python -m unittest -v test_experiment.py
      - name: Run smoke experiment
        working-directory: relay-systems/experiments/reward-conditioned-retrieval
        run: >-
          python experiment.py
          --train-episodes 1000
          --eval-episodes 250
          --seeds 2
          --output results
