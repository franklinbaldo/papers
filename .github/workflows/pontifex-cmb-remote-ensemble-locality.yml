name: Pontifex CMB multi-remote locality control

on:
  pull_request:
    paths:
      - "experiments/pontifex_torus/cmb_remote_ensemble_locality_holdout.py"
      - ".github/workflows/pontifex-cmb-remote-ensemble-locality.yml"
  workflow_dispatch:
    inputs:
      dev_skies:
        description: "Development base skies"
        required: false
        default: "12"
      heldout_skies:
        description: "Held-out base skies"
        required: false
        default: "24"

jobs:
  multi-remote-locality:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4

      - uses: astral-sh/setup-uv@v6
        with:
          enable-cache: true

      - name: Run multi-remote locality control
        run: |
          if [ "${{ github.event_name }}" = "pull_request" ]; then
            DEV=12
            HELD=24
          else
            DEV="${{ github.event.inputs.dev_skies || '12' }}"
            HELD="${{ github.event.inputs.heldout_skies || '24' }}"
          fi
          uv run experiments/pontifex_torus/cmb_remote_ensemble_locality_holdout.py \
            --dev-skies "$DEV" \
            --heldout-skies "$HELD" \
            --theta-per-cell 4 \
            --bins 3 \
            --beam-width-ratios 0.125 0.25 0.5 1.0 \
            --baseline-width 0.5 \
            --metric-k0 4 8 16 \
            --remote-shifts 0.25 0.5 0.75 \
            --ny 64 --nx 128 \
            --seed 20260923 \
            --rows /tmp/pontifex-cmb-remote-ensemble-locality.jsonl \
            --output /tmp/pontifex-cmb-remote-ensemble-locality.json

      - name: Summarize
        if: always()
        run: |
          if [ -f /tmp/pontifex-cmb-remote-ensemble-locality.json ]; then
            echo "## CMB multi-remote locality control" >> "$GITHUB_STEP_SUMMARY"
            echo '~~~json' >> "$GITHUB_STEP_SUMMARY"
            python -c 'import json; x=json.load(open("/tmp/pontifex-cmb-remote-ensemble-locality.json")); print(json.dumps({"design":x["design"],"development":x["development"],"heldout":x["heldout"]}, indent=2))' >> "$GITHUB_STEP_SUMMARY"
            echo '~~~' >> "$GITHUB_STEP_SUMMARY"
          fi

      - name: Upload
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: pontifex-cmb-remote-ensemble-locality
          path: |
            /tmp/pontifex-cmb-remote-ensemble-locality.json
            /tmp/pontifex-cmb-remote-ensemble-locality.jsonl
          if-no-files-found: warn
